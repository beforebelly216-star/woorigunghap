# 주토피 UI/UX 전면 재설계 — Design Direction v3

> 2026-08-26 사용자와 단계별 합의한 새 UI 방향. 기존 배포된 stock-theme UI가 만족스럽지 않아 전체 레이아웃을 다시 설계한다. 구현 전에 이 문서를 기준으로 세부 디자인을 확정한다.

## 전체 작업 단계

1. **Design Direction / Reference 확정**
   - 1A Brand Mood
   - 1B Layout Grammar
   - 1C Jootopi Character Rules
   - 1D Reference Board
2. **홈 화면만 전면 재설계**
   - Header / Hero / 무료 사주 진입 / 궁합 상품 진입 / 신뢰·설명 / CTA
   - 모바일 390px 우선
   - 홈 디자인 승인 전 다른 핵심 화면으로 확장하지 않는다.
3. **입력 UX 재설계**
   - `/free`, 1:1, 1:N
   - 생년월일/시간/관계 선택/상대 추가
4. **결제·생성 UX 재설계**
   - 상품 요약 / 결제 CTA / 생성 대기 / 이탈 후 복귀 / 실패·재시도
   - 서버 계산·결제·저장 로직은 유지하고 presentation layer 중심 수정

후속: 1:1 결과 → 1:N 결과 → 보관함/공유/계정 → multi-viewport QA.

---

## 1A Brand Mood — 확정 사항

### 1A-1 전체 방향
- **캐릭터 × 핀테크**.
- 주토피가 전체 디자인 인상의 중심이다.
- 귀여운 운세앱이나 HTS 복제품이 아니라, 정돈된 소비자용 분석 제품 위에 주토피의 캐릭터성과 금융 시각 언어를 결합한다.

### 1A-2 Color / Temperature
- **B 80% + C 20%**.
- 기본은 white / warm off-white.
- Jootopi Yellow는 브랜드 포인트.
- Ink/Black은 주요 텍스트와 primary CTA.
- 강한 Black × Yellow contrast는 결과 공개, 결제 완료, 공유 등 중요한 순간에 제한적으로 사용.

### 1A-3 Shape Language
- **Balanced**.
- 캐릭터는 둥글고 귀엽게, UI는 정돈되게.
- 큰 컨테이너 16~20px, 작은 데이터 영역 12~16px, 버튼 12~14px, 입력 10~12px 수준을 출발점으로 삼는다.
- pill은 chip 등에 제한하고 shadow는 최소화한다.
- 카드 반복보다 여백·divider·타이포 위계를 우선한다.

### 1A-4 Typography — B안 확정
- **캐릭터 × 핀테크 균형형**.
- 기본 UI, 본문, 숫자와 데이터는 정돈된 sans 계열로 유지한다.
- Hero 문구, 결과 공개, 주토피 강조 문구처럼 브랜드 감정이 필요한 제한된 지점만 display 성격을 강화한다.
- 기본 정보 순서: `핵심 숫자 → 의미 → 필요한 데이터 → 주토피 해석`.
- 구체 font family와 세부 scale은 구현 과정에서 현재 Pretendard 기반 호환성을 우선하며 필요 시 별도 확정한다.

### 1A-5 Character Presence
- **Moment-based 50% + Host 50%**.
- 장식 마스코트보다 사용자 여정을 함께 진행하는 호스트에 가깝다.
- 모든 카드마다 캐릭터를 붙이지 않는다.

### 1A-6 Voice / Personality
- 주토피 사용자 대사는 **무조건 반말**.
- 성격 비중: **귀여움 50 / 영리함 35 / 장난기 15**.
- 긍정적이고 호기심 많으며, 발견한 것을 신나게 알려주는 영리한 친구.
- 사용자를 평가하기보다 같이 보고 해석한다.
- **주식 밈 과다사용 금지**. 상한가/풀매수/손절/수급 같은 표현을 반복적으로 쓰지 않는다.
- 주식 요소는 말투보다 차트, 상승 화살표, 캔들, 태블릿, 데이터 표현, 의상/소품 등 시각 세계관에 둔다.
- 주식 드립은 정말 자연스러운 순간에 희소하게만 허용한다.

### 1A-7 Character Placement
- **Hero 20 / Companion 50 / Micro 30**.
- Hero: 홈, 결과 공개, 결제/공유 완료 등 중요한 장면.
- Companion: 무료/유료 결과 해설, 1:N 비교 등 주토피가 설명하는 장면.
- Micro: 입력 보조, TIP, 리스트 상태 등.

### 1A-8 Dialogue UX
- **Talk 20 / Commentary 65 / Scene 15**.
- Talk: 1~2문장 진행 대사.
- Commentary: 주토피의 핵심 역할. 보통 3~5문장으로 실제 관계 의미를 풀어준다.
- Scene: 결과 공개/생성/완료처럼 캐릭터와 큰 타이포가 화면을 장악하는 순간.
- 모든 대사를 말풍선에 넣지 않는다. 긴 Commentary는 일반 본문 레이아웃으로 처리한다.
- **UI가 객관 사실·점수·사주 근거를 보여주고, 주토피가 관계에서의 의미를 해석한다.**

### 1A-9 Iconography — B안 확정
- **Line + Solid 하이브리드**.
- 일반 탐색/보조 기능은 얇고 정돈된 line icon을 기본으로 한다.
- 핵심 상태, 선택, 주요 관계 의미는 제한적으로 solid 형태와 Jootopi Yellow를 사용한다.
- 캐릭터 일러스트를 일반 기능 아이콘 대용으로 반복 사용하지 않는다.

### 1A-10 Data Visualization — A안 확정
- **미니멀·클린 중심, 주식 요소 낮음**.
- 기본은 단순 line/bar/progress와 명확한 숫자 위계.
- 캔들·HTS형 gauge·상승/하락 ticker를 일반 관계 데이터의 기본 문법으로 사용하지 않는다.
- Black/Gray가 기본 데이터 언어이고 Yellow는 강조 포인트로 제한한다.
- 주식 시각언어는 캐릭터 세계관을 암시하는 보조 요소로만 사용한다.

### 1A-11 Motion / Interaction — A안 확정
- **미니멀 모션**.
- fade, 짧은 slide, 숫자 count, 간단한 chart draw 정도만 사용한다.
- 정보 전달 속도와 안정감을 우선한다.
- 캐릭터의 과한 반복 모션, confetti, drag 중심 인터랙션은 기본값으로 사용하지 않는다.
- `prefers-reduced-motion`을 유지한다.

---

## 1B Layout Grammar — A안 확정

- **정보 우선 구조 / 클린 카드 그리드**.
- 첫 화면에서 핵심 정보와 다음 행동을 바로 파악할 수 있어야 한다.
- 데이터·설명·CTA의 위계를 명확히 하고, 캐릭터는 정보 구조를 방해하지 않는 위치에 둔다.
- 카드 반복 자체가 목적이 되지 않도록 divider, whitespace, type hierarchy를 함께 사용한다.
- 모바일 390px을 우선하되 데스크톱에서는 폭을 무조건 480px로 제한하지 않는다.
- 홈에서 fake ranking, fake score, fake 관계 흐름 같은 실제 사용자 데이터처럼 보이는 장식 정보는 만들지 않는다.

## 1C Character Rules — B안 확정

- **적절한 캐릭터 균형 / 정보와 감정의 가이드**.
- 주토피는 화면의 주인공이 아니라 사용자가 정보를 이해하도록 돕는 가이드다.
- Hero·결과 공개·완료처럼 의미 있는 순간에는 존재감을 키울 수 있다.
- 일반 데이터 영역에서는 작은 companion/micro 수준 또는 필요한 경우에만 노출한다.
- 캐릭터가 점수, 계산 결과, CTA보다 시각적으로 우선하지 않는다.

## 1D Reference Board — 승인

최종 조합:

- Typography **B**
- Iconography **B**
- Data Visualization **A**
- Motion **A**
- Layout Grammar **A**
- Character Rules **B**

핵심 문장: **정보는 빠르고 명확하게, 주토피는 적재적소에.**

---

## Jootopi Identity Lock

사용자가 제공한 원본 캐릭터 시트를 **Character Source of Truth**로 사용한다.

### 변경 금지
- 얼굴 윤곽 및 얼굴/몸 비율
- 귀 길이·형태·위치
- 눈 형태·크기·위치
- 둥근 안경의 형태와 상대 크기
- 코/입 구성
- 볼/홍조 표현
- 손발 비율
- 기본 털 색감과 일러스트 선 특성

### 변경 가능
- 후드/티셔츠 색과 그래픽
- 포즈와 손동작
- 기존 구조 안에서의 표정
- 소품
- 차트/화살표/효과
- 작은 상황별 액세서리

### Production 규칙
- 기존 승인 포즈는 생성형 AI로 다시 그리지 않고 승인된 static asset을 사용한다.
- 신규 포즈는 원본 캐릭터 시트와 Identity Lock 기준으로 검수 후 공식 asset library에 편입한다.
- 원본 캐릭터 시트에서 분리한 13개 v1.1 asset이 가장 높은 신뢰도의 공식 자산이다.
- **캐릭터 이미지·신규 7포즈 최종 확정은 사용자 요청에 따라 후순위로 보류한다.**

---

## 2단계 홈 화면 — A안 승인

- 사용자 선택: **A안 / 정보 우선 클린 카드 그리드**.
- 실제 구현은 `free-first` 제품 결정을 최우선으로 유지한다.
- 홈 첫 CTA는 `무료로 내 관계 성향 보기`다.
- 1:1 / 1:N은 홈에서 가격과 차이를 설명할 수 있지만 직접 유료 전환 CTA는 두지 않는다.
- 무료 결과가 생성된 뒤 기존 전환 흐름에서 1:1 / 1:N을 선택한다.
- 주토피는 Hero 보조와 무료 분석 설명 역할에 제한적으로 배치한다.
- 실제 사용자 데이터가 없는 홈에서 fake score/ranking/chart를 만들지 않는다.

## 다음 단계

1. 홈 A안 코드 구현 및 390px 중심 검증.
2. 홈 승인 후 **3단계 입력 UX 재설계** — `/free` → 1:1 → 1:N 순서.
3. 캐릭터 시트/신규 포즈 asset 최종 검수는 별도 후순위 작업으로 유지.

## 현재 원칙

- 결제/사주 계산/AI 생성/저장/idempotency 등 기존 기능 로직은 UI 재설계 때문에 불필요하게 변경하지 않는다.
- 홈에서 확정한 디자인 언어를 사용자 승인 뒤 나머지 화면으로 확장한다.
- 기존 Production은 사용자 명시 승인 없이는 배포하지 않는다.
