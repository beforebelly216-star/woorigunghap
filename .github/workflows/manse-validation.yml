name: Manseryeok validation

on:
  push:
    branches: [main]
    paths:
      - "src/lib/manseryeok/**"
      - "src/app/api/manse/**"
      - "scripts/manse-*.ts"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/manse-validation.yml"
  pull_request:
    paths:
      - "src/lib/manseryeok/**"
      - "src/app/api/manse/**"
      - "scripts/manse-*.ts"
      - "package.json"
      - "package-lock.json"

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      KASI_SERVICE_KEY: ${{ secrets.KASI_SERVICE_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Run 30 golden cases
        run: npm run test:manse
      - name: Cross-check KASI when secret is configured
        run: npm run test:manse:kasi
      - name: Lint
        run: npm run lint
      - name: Production build
        run: npm run build
